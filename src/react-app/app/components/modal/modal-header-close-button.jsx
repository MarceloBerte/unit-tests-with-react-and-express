import React from 'react';

const ModalHeaderCloseButtonComponent = ({onClick}) => {
    return (
        <button type="button" onClick={onClick}>X</button>
    );
}
 
export default ModalHeaderCloseButtonComponent;
